# Agent Workspace

This directory is the sandbox where CodeForge creates and edits project files.

Try this task in the UI:

> Create a Python calculator module with add, subtract, multiply and divide functions. Add pytest tests, including division-by-zero behavior.

The coding agent is intentionally restricted to this directory.
